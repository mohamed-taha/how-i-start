require 'spec_helper'

describe HowIStart do
  it 'has a version number' do
    expect(HowIStart::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
